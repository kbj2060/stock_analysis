from setuptools import setup, find_packages

setup(name='system_trading',
      version='0.1.1',
      author='llewyn',
      description='Analysis of Stock',
      packages=find_packages(),
      long_description=open('README.md').read(),
      zip_safe=False,
      setup_requires=[]
)